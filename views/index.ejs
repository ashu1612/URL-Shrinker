<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="styles.css"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
    rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
    crossorigin="anonymous">

    <title>URL Shrinker</title>
</head>

<body>
 <div class="container">
    <h1 class="text-center">URL Shrinker</h1>
    <form action="shortUrls" method="POST" class="my-4 form-inline">
        <input required placeholder="URL" type="url" name="fullUrl" id="fullUrl" class="form-control col mr-2">
        <br>
        <div class="text-center">
            <button id="submit" class="btn btn-success text-center" type="submit">Shrink</button>   
        </div>
    </form>

    <hr class="mb-5"/>

    <div id="shortcode" class="mydiv"></div>

    

    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                <td>Full URL</td>
                <td>Short URL</td>
                <td>Clicks</td>
            </tr>
        </thead>
        <tbody>
            <% shortUrls.forEach(shortUrl => { %>
                <tr>
                    <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                    <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                    <td><%= shortUrl.clicks %></td>
                </tr>    
            
            <% }) %> 
        </tbody>
    </table>
 </div>

 <!-- <script>
     $(function () {
    $('#submit').click(function (shortUrl) {
            $('#shortcode').html("Short URL: " + '<a href="/' + shortUrl.short + '">'+window.location.href+shortUrl.short + '</a>');
        })
})
 </script> -->


    
</body>
</html>